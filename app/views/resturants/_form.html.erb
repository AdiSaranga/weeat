<%= form_with(model: resturant, local: true) do |form| %>
  <% if resturant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(resturant.errors.count, "error") %> prohibited this resturant from being saved:</h2>

      <ul>
      <% resturant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :resturant_name %>
  </div>

  <div class="field">
    <%= form.label :cuisine %>
    <%= form.text_field :cuisine, id: :resturant_cuisine %>
  </div>

  <div class="field">
    <%= form.label :rate %>
    <%= form.text_field :rate, id: :resturant_rate %>
  </div>

  <div class="field">
    <%= form.label :10bis_enabled %>
    <%= form.check_box :10bis_enabled, id: :resturant_10bis_enabled %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :resturant_address %>
  </div>

  <div class="field">
    <%= form.label :last_delivery_time %>
    <%= form.time_select :last_delivery_time, id: :resturant_last_delivery_time %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
